# Frontend Environment Variables

# API Base URL - Leave empty for production (uses relative URLs through nginx proxy)
# Set to backend URL for local development
VITE_API_BASE_URL=http://172.23.14.24:3049
